import n from "./bus.es.js";
import c from "./renderVnodeCom.es.js";
import { openBlock as m, createElementBlock as f, normalizeClass as v, normalizeStyle as g, createElementVNode as p, renderSlot as y } from "vue";
import "./waterfall.vue.es2.js";
import w from "./_virtual/_plugin-vue_export-helper.es.js";
const E = {
  props: {
    col: {
      type: Number,
      default: 2
    },
    width: Number,
    height: {
      type: String
      // default:'100vh',    取消height默认值
    },
    data: {
      type: Array,
      default: []
    },
    gutterWidth: {
      type: Number,
      default: 10
    },
    isTransition: {
      type: Boolean,
      default: !0
    },
    lazyDistance: {
      type: Number,
      default: 300
    },
    loadDistance: {
      type: Number,
      default: 300
    }
  },
  data() {
    return {
      root: null,
      columns: [],
      loadmore: !0,
      timeout: null,
      lazyTimeout: null,
      lastScrollTop: 0,
      timer: null,
      loadedIndex: 0,
      columnWidth: 0,
      isresizing: !1,
      clientHeight: document.documentElement.clientHeight || document.body.clientHeight,
      clientWidth: document.documentElement.clientWidth || document.body.clientWidth
    };
  },
  computed: {
    trueLazyDistance() {
      return this.clientWidth / 375 * this.lazyDistance;
    },
    max() {
      return this.clientWidth / 375 * this.loadDistance;
    },
    viewHeight() {
      return this.height ? this.root.offsetHeight : this.clientHeight;
    }
  },
  watch: {
    col(t) {
      this.$nextTick(() => {
        this.init();
      });
    },
    data(t, e) {
      this.$nextTick(() => {
        clearTimeout(this.timer), this.timer = setTimeout(() => {
          this.isresizing || (t.length < this.loadedIndex && (this.loadedIndex = t.length), this.resize(this.loadedIndex > 0 ? this.loadedIndex : null));
        }, 300);
      });
    }
  },
  methods: {
    init() {
      this.root = this.$refs.vueWaterfall, this.clearColumn();
      for (var t = parseInt(this.col), e = 0; e < t; e++) {
        let i = document.createElement("div");
        if (i.className = "vue-waterfall-column", this.width)
          i.style.width = this.width + "px", e != 0 && (i.style.marginLeft = this.gutterWidth + "px"), this.columnWidth = this.width;
        else if (this.gutterWidth && !this.width) {
          const o = Math.floor(
            (this.root.offsetWidth - (t - 1) * this.gutterWidth) / t
          );
          e != 0 && (i.style.marginLeft = this.gutterWidth + "px"), i.style.width = o + "px", this.columnWidth = o;
        } else
          i.style.width = 100 / parseInt(t) + "%", this.columnWidth = 100 / parseInt(t) / 100 * document.documentElement.clientWidth;
        this.root || (this.root = this.$refs.vueWaterfall), this.root && this.root.appendChild(i), this.columns.push(i);
      }
      this.resize();
    },
    async __setDomImageHeight(t) {
      if (t.getElementsByTagName)
        for (var e = t.getElementsByTagName("img"), i = 0; i < e.length; i++) {
          var o = e[i].getAttribute("lazy-src");
          if (!e[i].getAttribute("src") && o) {
            var r = new Image();
            if (r.src = o, r.complete) {
              var s = e[i].offsetWidth || this.columnWidth, l = r.height * s / r.width;
              s && (e[i].style.height = l + "px");
            } else
              await new Promise((a, d) => {
                r.onload = function() {
                  var h = e[i].offsetWidth || this.columnWidth, u = r.height * h / r.width;
                  h && (e[i].style.height = u + "px"), a();
                }, r.onerror = function() {
                  a();
                };
              });
          }
        }
    },
    async append(t) {
      var e = this;
      if (this.columns.length > 0) {
        let o = this.columns[0];
        for (var i = 1; i < this.columns.length; i++)
          await e.__getHeight(o) > await e.__getHeight(e.columns[i]) && (o = e.columns[i]);
        await this.__setDomImageHeight(t), o && o.appendChild(t);
      }
    },
    checkImg(t) {
      return t ? !!(t.getElementsByTagName && t.getElementsByTagName("img").length) : !1;
    },
    async resize(t, e) {
      this.isresizing = !0, this.routeChanged = !1;
      var i = this;
      const o = this.$slots.default()[0].children;
      if (!(o != null && o.length)) {
        this.isresizing = !1;
        return;
      }
      for (!t && t != 0 && !e ? (e = await c(o), this.loadedIndex = 0, this.clear()) : e || (this.loadedIndex = t, e = (await c(o)).splice(t)); e.length; ) {
        if (this.routeChanged) {
          console.warn("路由发生变化，<vue-waterfall>组件停止渲染");
          break;
        }
        const l = e.shift();
        if (l && i.checkImg(l)) {
          var r = l.getElementsByTagName("img"), s = new Image();
          s.src = r[0].getAttribute("src") || r[0].getAttribute("lazy-src"), s.complete ? (await i.append(l), i.lazyLoad(r)) : await new Promise((a, d) => {
            s.onload = async function() {
              await i.append(l), i.lazyLoad(r), a();
            }, s.onerror = async function(h) {
              await i.append(l), i.lazyLoad(r), a();
            };
          });
        } else
          await i.append(l);
        i.loadedIndex++;
      }
      this.isresizing = !1, i.loadmore = !0, i.$emit("finish");
    },
    computedPx(t, e) {
      t.style.width = e.width / this.columnWidth;
    },
    lazyLoad(t) {
      if (t || (this.root || (this.root = this.$refs.vueWaterfall), t = this.root && this.root.getElementsByTagName("img")), !(!t || t.length < 0))
        for (var e = 0; e < t.length; e++)
          t[e].className.match("animation") && t[e].getAttribute("src") || (t[e].className.match("animation") && !t[e].getAttribute("src") ? (t[e].src = t[e].getAttribute("lazy-src"), t[e].removeAttribute("lazy-src")) : t[e].getAttribute("src") && !t[e].className.match("animation") ? t[e].className = t[e].className + " animation" : !t[e].getAttribute("src") && t[e].getBoundingClientRect().top < this.viewHeight + this.trueLazyDistance && (t[e].src = t[e].getAttribute("lazy-src"), t[e].className = t[e].className + " animation", t[e].removeAttribute("lazy-src")));
    },
    clearColumn() {
      this.columns.forEach((t) => {
        t.remove();
      }), this.columns = [];
    },
    clear() {
      this.columns.forEach((t) => {
        t.innerHTML = "";
      });
    },
    mix() {
      var t = this.$refs.vueWaterfallSlotBox.cloneNode(!0).children;
      t.sort(() => Math.random() - 0.5), this.resize(0, t);
    },
    async __getHeight(t) {
      return t.offsetHeight;
    },
    __emitLoadMore() {
      if (!this.root)
        return;
      var t = this;
      const e = this.height ? this.root.scrollTop : document.documentElement.scrollTop || document.body.scrollTop, i = this.height ? this.root.scrollHeight : document.documentElement.offsetHeight;
      var o = i - e - this.viewHeight;
      t.$emit("scroll", {
        scrollHeight: i,
        scrollTop: e,
        viewHeight: this.viewHeight,
        clientHeight: t.clientHeight,
        diff: o,
        time: Date.now()
      }), o < t.max && t.loadmore && i > this.viewHeight && (t.lastScrollTop = e, t.loadmore = !1, t.$emit("loadmore")), clearTimeout(t.lazyTimeout), t.lazyTimeout = setTimeout(function() {
        t.lazyLoad();
      }, 14);
    },
    handleScroll() {
      this.__emitLoadMore();
    },
    initScrollEvents() {
      this.height ? (this.root.addEventListener("scroll", this.handleScroll), this.root.addEventListener("touchmove", this.handleScroll)) : (document.addEventListener("scroll", this.handleScroll), document.addEventListener("touchmove", this.handleScroll));
    },
    destoryScrollEvents() {
      this.root && this.root.removeEventListener("scroll", this.handleScroll), this.root && this.root.removeEventListener("touchmove", this.handleScroll), this.root && (this.root.onresize = null), window.onresize = null, document.removeEventListener("scroll", this.handleScroll), document.removeEventListener("touchmove", this.handleScroll);
    },
    // 监听路由是否发生变化
    __listenRouterChange() {
      var t = function(i) {
        var o = history[i];
        return function() {
          var r = o.apply(this, arguments), s = new Event(i);
          return s.arguments = arguments, window.dispatchEvent(s), r;
        };
      };
      ["replaceState", "pushState"].forEach((i) => {
        window.addEventListener(i, () => {
          this.routeChanged = !0;
        }), window.history[i] = t(i);
      }), window.addEventListener("popstate", () => {
        this.routeChanged = !0;
      });
    }
  },
  destroyed() {
    this.destoryScrollEvents();
  },
  unmounted() {
    this.destoryScrollEvents();
  },
  beforeCreate() {
    n.on("forceUpdate", () => {
      this.resize();
    }), n.on("mix", () => {
      this.mix();
    });
  },
  mounted() {
    this.__listenRouterChange(), this.$nextTick(() => {
      this.init(), this.initScrollEvents();
    });
  },
  activated() {
    this.initScrollEvents();
  },
  deactivated() {
    this.destoryScrollEvents();
  }
}, z = {
  class: "slot-box",
  ref: "vueWaterfallSlotBox"
};
function W(t, e, i, o, r, s) {
  return m(), f("div", {
    class: v(["vue-waterfall", i.isTransition && "is-transition"]),
    style: g({ height: i.height }),
    ref: "vueWaterfall",
    id: "vueWaterfall"
  }, [
    p("div", z, [
      y(t.$slots, "default")
    ], 512)
  ], 6);
}
const L = /* @__PURE__ */ w(E, [["render", W]]);
export {
  L as default
};
